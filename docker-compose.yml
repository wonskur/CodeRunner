services:
  forge-api:
    build: ./forge-api
    ports:
      - "8000:8000"
    volumes:
      - ./forge-api:/app
      - ./forge-engine:/forge-engine
    environment:
      - PYTHONPATH=/app:/forge-engine
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload